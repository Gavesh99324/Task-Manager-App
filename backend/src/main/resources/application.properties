# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration — MongoDB Atlas
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://gtj99324_db_user:2JdirthI4ToMveDF@cluster0.z6emqgo.mongodb.net/taskmanager?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DB:taskmanager}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:thisIsAVeryLongSecretKeyForJWTSigningThatShouldBeAtLeast256BitsLongForHS256Algorithm}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Pagination Defaults
app.pagination.default-page-size=10
app.pagination.max-page-size=100

# CORS Configuration
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Logging
logging.level.com.taskmanager=INFO
logging.level.org.springframework.security=WARN
